<!-- Header -->
<{includeq file='db:contact_admin_header.tpl'}>
  <{if $prune}>
    <div class="contact">
      <{$prune}>
    </div>
    <{/if}>
      <!-- Footer -->
      <{includeq file='db:contact_admin_footer.tpl'}>